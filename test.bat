
curl http://localhost/

curl http://localhost/test

curl http://localhost/data

curl http://localhost/data -d "a"
curl http://localhost/data -d "b"
curl http://localhost/data -d "c"
curl http://localhost/data -d "d"
